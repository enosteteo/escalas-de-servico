// Placeholder — app-level widget tests require platform rendering.
// All domain logic is covered by the feature tests.
import 'package:flutter_test/flutter_test.dart';

void main() {
  test('placeholder', () {
    expect(true, isTrue);
  });
}
